import styled from "styled-components";

const Intro = () => {
  return <Stcontainer>Intro</Stcontainer>;
};

export default Intro;

/*반응형 맞춤 */
const Stcontainer = styled.div`
  width: 100%;
  position: absolute;
  overflow: auto;
  height: calc(100vh - 5rem);
`;
